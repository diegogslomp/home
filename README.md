# home

```bash
sudo apt install git -y
mkdir ~/repos && cd ~/repos
git clone https://github.com/diegogslomp/home.git
cd home
bash install
```

```bash
Run `bash install function`:

bat
check-dns-servers
disable-ipv6
docker
docker-compose
go
help
home
kubectl
nodejs
pathogen
python
terminator-themes
vim-sensible
vim-surround
vundle
woeusb
youcompleteme
```
